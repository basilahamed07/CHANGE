# Exclusions — companies/claims banned from pursuit or generation.
# DO_NOT_USE claims NEVER appear in generated artifacts and are hard-blocked
# by the EvidenceChecker even if an LLM tries to include them.

claims:
  - id: exclusion_example
    value: ""
    status: DO_NOT_USE
    source: ""
    notes: "Example: a company you never worked at but an LLM once hallucinated. Add real exclusions below."
